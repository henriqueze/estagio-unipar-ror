<%= form_with(model: backoffice_category, local: true) do |form| %>
  <% if backoffice_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(backoffice_category.errors.count, "error") %> prohibited this backoffice_category from being saved:</h2>

      <ul>
      <% backoffice_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
