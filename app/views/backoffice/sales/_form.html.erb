<%= form_with(model: backoffice_sale, local: true) do |form| %>
  <% if backoffice_sale.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(backoffice_sale.errors.count, "error") %> prohibited this backoffice_sale from being saved:</h2>

      <ul>
      <% backoffice_sale.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :date %>
    <%= form.datetime_select :date %>
  </div>

  <div class="field">
    <%= form.label :total_value %>
    <%= form.text_field :total_value %>
  </div>

  <div class="field">
    <%= form.label :discount_value %>
    <%= form.text_field :discount_value %>
  </div>

  <div class="field">
    <%= form.label :state %>
    <%= form.text_field :state %>
  </div>

  <div class="field">
    <%= form.label :person_id %>
    <%= form.text_field :person_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
